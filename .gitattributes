# Store and check out text files with LF endings on every OS, so shell
# scripts (entrypoint.sh) and YAML behave the same on Windows and in Linux
# containers and CI.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
